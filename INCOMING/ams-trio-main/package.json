{
  "name": "ams-trio",
  "version": "0.1.0",
  "description": "AMS Trio — a conversational, multi-pane interface for the trio-pattern. Each avatar is its own Claude Agent SDK session with its own persona, model, and office.",
  "type": "module",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.177",
    "express": "^4.21.2"
  }
}
